# Architecture

Monorepo with React web app, NestJS API, shared contracts package, and PostgreSQL via Prisma.

Backend is a modular monolith with deterministic compliance rules and PDF generation through Playwright.
