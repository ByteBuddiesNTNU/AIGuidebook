export function AdminGuidelinesPage() {
  return (
    <section className="card">
      <h2>Admin Guidelines</h2>
      <p>Create/publish guideline sets via admin API endpoints in this initial version.</p>
    </section>
  );
}
